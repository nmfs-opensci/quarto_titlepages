To use,

Either

1. Replace `_titlepage.tex` in the folder above this with one of the `_titlepage-xyz.tex` here. You need to use the name `_titlepage.tex`.

Or

1. Copy the file `_titlepage-xyz.tex` to the folder above this.
2. Change the lines with `$_titlepage.tex()$` in `before-body.tex` to $_titlepage-xyz.tex()$`
3. Change `"template/_titlepage.tex"` in the `article.qmd` YAML to `"template/_titlepage-xyz.tex"`

