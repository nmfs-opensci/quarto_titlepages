$if(has-frontmatter)$
  \begin{frontmatter}
$endif$

\begin{titlepage}
$-- % Coverpage
$if(coverpage)$
$_coverpage.tex()$
$endif$

$-- % Titlepage
$if(titlepage)$
% Use the file
$if(titlepage-file)$
\input{$titlepage-filename$}
$else$
$_titlepage.tex()$
$endif$
$endif$
\end{titlepage}
\setcounter{page}{1}
$if(has-frontmatter)$
\end{frontmatter}
$endif$
